<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/listHeight"
    android:orientation="vertical"
    android:layout_marginTop="@dimen/listTextMargin"
    android:layout_marginBottom="@dimen/listTextMargin"
    android:layout_marginLeft="@dimen/activity_horizontal_margin"
    android:layout_centerVertical="true">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/listTitleSize"
        android:text="Yay"
        tools:text="Song Name"/>

    <TextView
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/listSubtitleSize"
        tools:text="Artist Name"/>

</LinearLayout>